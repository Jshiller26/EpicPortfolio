import { ImageResponse } from 'next/og';
import { NextRequest } from 'next/server';

export async function GET(request: NextRequest) {
  return new ImageResponse(
    (
      <img
        src="/images/RayrayIcon.webp"
        alt="JshillerOS Icon"
        style={{ width: '100%', height: '100%' }}
      />
    ),
    {
      width: 32,
      height: 32,
    }
  );
}
